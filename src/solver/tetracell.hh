#pragma once

class tetra_cell
{
    public:
        tetra_cell(int up, int down, int right, left)
        {
            this.up = up;
            this.down = down;
            this.right = right;
            this.left = left;
        }
        int up;
        int down;
        int right;
        int left;
};

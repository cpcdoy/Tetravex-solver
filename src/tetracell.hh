#pragma once

class TetraCell
{
    public:
        TetraCell(int up, int down, int right, left)
        {
            this.up = up;
            this.down = down;
            this.right = right;
            this.left = left;
        }
        int up;
        int down;
        int right;
        int left;
};
